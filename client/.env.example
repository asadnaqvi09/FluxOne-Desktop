# FluxOne POS — client (Vite) environment template
# Copy to client/.env for local Vite dev. Never commit client/.env.

# POS API base URL (must include /api)
VITE_API_BASE_URL=http://127.0.0.1:3000/api
